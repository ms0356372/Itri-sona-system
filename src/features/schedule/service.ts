import {requireSupabase} from '../../lib/supabase'; import type {ImportResult,Participant} from '../../types'; import type {ScheduleRow} from './excel';
type ParticipantRow={id:string;session_id:string;sequence_no:number;national_id:string;employee_no:string;full_name:string;gender:string;schedule_slot:string;group_code:Participant['groupCode'];planned_items:string[];checkin_no:string|null;status:Participant['status'];checked_in_at:string|null;called_at:string|null;note:string;updated_at:string};
export const mapParticipant=(r:ParticipantRow):Participant=>({id:r.id,sessionId:r.session_id,sequence:r.sequence_no,nationalId:r.national_id,employeeNo:r.employee_no,name:r.full_name,gender:r.gender,slot:r.schedule_slot,groupCode:r.group_code,plannedItems:r.planned_items,checkinNo:r.checkin_no,status:r.status,checkedInAt:r.checked_in_at,calledAt:r.called_at,note:r.note,updatedAt:r.updated_at});
export async function listParticipants(sessionId:string){const{data,error}=await requireSupabase().from('participants').select('*').eq('session_id',sessionId).order('sequence_no');if(error)throw error;return(data as ParticipantRow[]).map(mapParticipant);}
export async function importSchedule(sessionId:string,rows:ScheduleRow[]):Promise<ImportResult>{const existing=await listParticipants(sessionId);const keys=new Set(existing.flatMap(p=>[`${p.employeeNo}|employee`,`${p.nationalId}|national`]));const unique:ScheduleRow[]=[];const batch=new Set<string>();for(const row of rows){const ek=`${row.employeeNo}|employee`,nk=`${row.nationalId}|national`;if(keys.has(ek)||keys.has(nk)||batch.has(ek)||batch.has(nk))continue;batch.add(ek);batch.add(nk);unique.push(row);}if(unique.length){const payload=unique.map(r=>({session_id:sessionId,sequence_no:r.sequence,national_id:r.nationalId,employee_no:r.employeeNo,full_name:r.name,gender:r.gender,schedule_slot:r.slot,group_code:r.groupCode,planned_items:r.items}));const{error}=await requireSupabase().from('participants').insert(payload);if(error)throw error;}return{inserted:unique.length,skipped:rows.length-unique.length};}
